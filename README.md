# Лабораторна робота №2: Еволюція TypeScript-проєкту

Це навчальний проєкт, створений в рамках лабораторної роботи №2. Він демонструє повний життєвий цикл невеликої TypeScript-бібліотеки, починаючи з ініціалізації проєкту і закінчуючи випуском версії 2.0.0

Проєкт налаштований з використанням сучасних інструментів для розробки та контролю якості коду, таких як:
* TypeScript - для суворої типізації
* ESLint - для пошуку помилок та стилю коду
* Prettier - для автоматичного форматування
* Husky - для git-хуків
* Commitlint - для валідації повідомлень комітів
* zod - для валідації змінних оточення

---

## 🚀 Інструкції з запуску

1.  **Встановлення залежностей:**
    ```bash
    npm i
    ```

2.  **Запуск демонстраційного файлу:**
    (Цей файл використовує `tsx` для запуску TypeScript "на льоту")
    ```bash
    npm run demo
    ```

3.  **Збірка проєкту:**
    (Створює фінальні `.js` та `.d.ts` файли у папці `/dist`)
    ```bash
    npm run build
    ```

---

## Приклади використання

Весь демонстраційний код знаходиться у файлі `src/demo.ts`.

```typescript
import { add, capitalize, formatNumber, groupBy, Logger, type LogLevel, type User } from './index.js';
import { config } from './config.js';

// v2.0.0 (з масивом)
console.log('sum(2.0 ok):', add([2, 3, 4]));

console.log('capitalize(typed):', capitalize('hello'));

// Використовує APP_PRECISION з .env
console.log('format(from .env):', formatNumber(123.456));

// Generic groupBy
const users: User[] = [
  { id: 1, name: 'Alice' },
  { id: 2, name: 'Bob' },
];
console.log('group ok:', groupBy(users, 'name'));

// Клас Logger та конфіг
const logger = new Logger(config.LOG_LEVEL as LogLevel);
logger.info('Application started');
logger.debug('Extra debug info'); 
```
---
# Налаштування (.env)
Для коректної роботи Logger та formatNumber проєкту необхідно створити файл .env у корені. Цей файл не повинен потрапляти в репозиторій (він вже є у .gitignore)
Він очікує на такі ключі:
* APP_PRECISION (Число від 0 до 10, за замовчуванням 2). Визначає точність для formatNumber.
* LOG_LEVEL (Один із: 'silent', 'info', 'debug', за замовчуванням 'info'). Визначає рівень логування для Logger.

## Приклад .env:
```code
APP_PRECISION=3
LOG_LEVEL=debug
```
---
# Еволюція версій
### Проєкт розвивався покроково, симулюючи реальний процес розробки:
* v0.1.0: Початкова версія. Додано базові функції add та capitalize з типом any
* v0.2.0: Покращення типізації. any замінено на конкретні типи number та string
* v0.3.0: Нова функція. Додано formatNumber зі складним опційним типом NumberFormatOptions
* v0.4.0: Нові можливості. Додано interface User та узагальнену (generic) функцію groupBy
* v0.5.0: Робота з .env. Додано class Logger, валідацію конфігу через zod та читання .env
* v1.0.0: Стабілізація API. Заборонено any (правило ESLint error), додано exports у package.json
* v2.0.0: Breaking Change! Сигнатура (спосіб виклику) функції add змінена з (a, b) на (values: number[])
---
# Релізи
Всі кроки еволюції доступні як теги на GitHub:

[Переглянути всі релізи](https://github.com/OlenaKast/lab2/releases)
